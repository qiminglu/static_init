# static_init

## Build

```
git submodule init
git submodule update
mkdir build
cd build
cmake ..
make -j4
```

## C++ version

```
cd build
./cxx_load
```

## Python module

```
cd build
python3 -c 'import pylsexpr'
```
